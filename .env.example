# Example .env for the realtime bus demo
# Copy this file to .env and fill in values before running services

# MQTT broker
BROKER_HOST=localhost
BROKER_PORT=1883

# Postgres DSN (one of these):
# Option A: full DSN
PG_DSN=dbname=realtime_demo user=postgres password=postgres host=localhost port=5432
# Option B: DATABASE_URL (alternative env name some tools use)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/realtime_demo

# Mapbox token used by the front-end
MAPBOX_TOKEN=your_mapbox_token_here
